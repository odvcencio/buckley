outcome ModelChoice {
    action: string
    model: string
}

strategy model_select returns ModelChoice {
    when {
        task.phase == "planning"
        and model.supports_reasoning
    } then PlanningReasoner {
        action: "use",
        model: "z-ai/glm-5.2",
    }

    when {
        task.phase == "execution"
    } then ExecutionModel {
        action: "use",
        model: "z-ai/glm-5.2",
    }

    when {
        task.phase == "review"
        and model.supports_reasoning
    } then ReviewReasoner {
        action: "use",
        model: "z-ai/glm-5.2",
    }

    else DefaultModel {
        action: "use",
        model: "z-ai/glm-5.2",
    }
}
