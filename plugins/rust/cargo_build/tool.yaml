name: cargo_build
description: Build Rust project using cargo
parameters:
  type: object
  properties:
    release:
      type: boolean
      description: Build in release mode
      default: false
    package:
      type: string
      description: Specific package to build (optional)
      default: ""
  required: []
executable: ./cargo_build.sh
timeout_ms: 600000
